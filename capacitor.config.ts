import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.alphacarbo.app',
  appName: 'AlphaCarbo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
